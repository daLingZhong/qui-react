import { clsnamePrefix } from '@/var'

export const clsPrefix = `${clsnamePrefix}-pagination`
export const clsPrefixPager = `${clsnamePrefix}-pager`

export const PAGER_TYPE = {
  page: 'page',
  prev: 'prev',
  next: 'next',
  jumpPrev: 'jumpPrev',
  jumpNext: 'jumpNext',
}
