import { clsnamePrefix } from '@/var'
export const clsPrefix = `${clsnamePrefix}-select`
export const clsPrefixCas = `${clsnamePrefix}-cascade`
export const clsPrefixTree = `${clsnamePrefix}-tree`
export const LINE_HEIGHT = {
  large: '36px',
  default: '32px',
  small: '28px',
}
